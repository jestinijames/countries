/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["restcountries.eu", "flagcdn.com", "upload.wikimedia.org", "restcountries.com"],
      },
};

export default nextConfig;
